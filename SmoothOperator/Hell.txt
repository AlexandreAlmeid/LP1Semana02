dotnet new console-classic --nrt=false --langVersion 8.0 --name SmoothOperator

dotnet sln add SmoothOperator

# para adicionar ao guit

git add .

git commit -m "Add SmoothOperator"

git push origin main
